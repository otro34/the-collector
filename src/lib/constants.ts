/**
 * Collection configuration constants
 */

/**
 * Maximum number of items to load when using virtual scrolling.
 * This limit is applied to all collection pages (books, music, videogames).
 */
export const VIRTUAL_SCROLL_LIMIT = 1000
